function ModeButton({ children }) {
  return (
    <button className="border-2 border-blue-500 text-blue-500 text-sm px-2 py-0.5 rounded-md">
      {children}
    </button>
  );
}

export default ModeButton;
